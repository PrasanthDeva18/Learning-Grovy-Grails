def reader = System.in.newReader()

int a = reader.readLine() as Integer;
int b = reader.readLine() as Integer;

println "A + B : ${a+b}"
println "A - B : ${a-b}"
println "A * B : ${a*b}"
println "A / B : ${a/b}"
println "A % B : ${a%b}"
